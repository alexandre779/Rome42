window.R42_ENGINE=(()=>{const pad=n=>String(n).padStart(2,'0');const iso=d=>`${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}`;const monday=d=>{const x=new Date(d);x.setHours(12,0,0,0);const day=(x.getDay()+6)%7;x.setDate(x.getDate()-day);return x};const add=(d,n)=>{const x=new Date(d);x.setDate(x.getDate()+n);return x};const phase=(i,n)=>{const r=i/Math.max(1,n-1);return r<.2?'Base':r<.55?'Construction':r<.82?'Spécifique marathon':'Affûtage'};const level=p=>{const km=+p.weeklyKm||0,long=+p.longestRunKm||0;if(km>=40&&long>=18)return'advanced';if(km>=20&&long>=10)return'intermediate';return'beginner'};const ratio=(p,w,n)=>{if(p.goalType==='time'&&level(p)!=='beginner')return'course facile / marche selon besoin';const r=w/Math.max(1,n-1);if(level(p)==='beginner')return r<.2?'1 min course / 2 min marche':r<.5?'3 min / 2 min':r<.8?'5 min / 2 min':'ratio marathon testé';return r<.5?'9 min course / 1 min marche':'ratio marathon testé'};function longMin(p,w,n){const l=level(p),start=l==='advanced'?105:l==='intermediate'?85:60,peak=l==='advanced'?180:l==='intermediate'?195:210;const taper=w>=n-3;if(taper){const seq=[135,90,45];return seq[Math.min(2,w-(n-3))]}const prog=Math.min(1,w/Math.max(1,n-4));let v=start+(peak-start)*prog;if((w+1)%4===0)v*=.78;return Math.round(v/5)*5}function quality(p,ph){if(p.goalType==='finish'||level(p)==='beginner')return ph==='Spécifique marathon'?'Run/walk soutenu contrôlé 45–55 min · RPE 4–5':'Run/walk facile 35–50 min · RPE 3–4';if(ph==='Base')return'Footing facile 45–55 min · RPE 3–4';if(ph==='Construction')return'Tempo contrôlé : 15 min facile + 3 × 8 min RPE 5–6, récup 3 min + retour au calme';return'Séance spécifique marathon : 15 min facile + 3 × 12 min allure cible contrôlée + retour au calme'}function makeWeek(p,w,n,startDate){const ph=phase(w,n),rat=ratio(p,w,n),L=longMin(p,w,n),avail=(p.days||[2,4,6]).map(Number).sort(),longDay=Number(p.longDay??6);let runDays=avail.slice();if(!runDays.includes(longDay))runDays.push(longDay);runDays=[...new Set(runDays)].sort();const slots={};runDays.forEach((d,j)=>slots[d]=j);const workouts=runDays.map((d,j)=>{if(d===longDay)return`Sortie longue ${Math.floor(L/60)?Math.floor(L/60)+' h ':''}${L%60?L%60+' min':''} · ${rat} · RPE 3–4 · ravitaillement à tester au-delà de 90 min`;if(runDays.length>=4&&j===1)return quality(p,ph);if(runDays.length>=5&&j===2)return ph==='Spécifique marathon'?'Sortie moyenne 60–75 min facile':'Footing facile 45–60 min';return ph==='Affûtage'?'Footing très facile 25–45 min':'Endurance facile 35–55 min · RPE 3–4'});const days=[];for(let i=0;i<7;i++){const d=add(startDate,i);let training='Repos / mobilité douce 10–15 min';if(runDays.includes(i)){training=workouts[runDays.indexOf(i)];if(p.strength==='yes'&&i!==longDay&&runDays.indexOf(i)===0)training+=' + renforcement 20–30 min si frais';}days.push({date:iso(d),training,menu:'GEN',nutritionNote:i===longDay?'Avant : repas digeste. Pendant >90 min : tester progressivement 30–60 g de glucides/h et boire selon soif/météo.':'Hydratation normale, protéines à chaque repas, glucides adaptés à la charge.'})}return{week:w+1,phase:ph,ratio:rat,days}}function generate(p){const race=new Date((p.raceDate||'2027-03-11')+'T12:00:00'),start=monday(new Date()),weeks=Math.max(1,Math.ceil((race-start)/(7*86400000))+1),arr=[];for(let w=0;w<weeks;w++)arr.push(makeWeek(p,w,weeks,add(start,w*7)));return arr}return{generate,level};})();