(()=>{function kind(name,unit){const n=(name||'').toLowerCase();if(n.includes('cabillaud')||n.includes('saumon')||n.includes('poisson'))return'fish';if(n.includes('poulet')||n.includes('dinde')||n.includes('bœuf'))return'meat';if(n.includes('œuf'))return'eggs';if(n.includes('riz')||n.includes('pâtes')||n.includes('semoule')||n.includes('pommes de terre')||n.includes('pain'))return'carb';if(n.includes('huile')||n.includes('amandes')||n.includes('noix'))return'fat';if(n.includes('légume')||n.includes('courgette')||n.includes('tomates /')||n==='fruit'||n==='banane')return'produce';return'other'}
function factor(profile,baseScale,type){if(type==='fish'||type==='meat'||type==='eggs')return 1;if(type==='produce')return Math.max(.85,Math.min(1.1,baseScale));if(type==='carb')return Math.max(.75,Math.min(1.45,baseScale));if(type==='fat')return Math.max(.85,Math.min(1.25,baseScale));return Math.max(.8,Math.min(1.25,baseScale))}
function cap(name,q,u){const t=kind(name,u);if(t==='fish'&&u==='g')return Math.min(q,150);if(t==='meat'&&u==='g')return Math.min(q,180);if(t==='eggs'&&u==='pièce')return Math.min(q,3);return q}
window.R42_PORTIONS={kind,factor,cap};})();