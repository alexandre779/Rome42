(()=>{const N=R42_NUTRITION;let nWeek=null,nSpan=1,nPeople=2;const pattern=['C','A','B','C','D','F','G'];const fmtShort=x=>new Intl.DateTimeFormat('fr-FR',{weekday:'short',day:'numeric',month:'short'}).format(new Date(x+'T12:00:00'));function weeksNow(){try{return plan()}catch(e){return APP_DATA.weeks}}function profile(){try{return P}catch(e){return null}}function codeFor(day,idx){const p=profile();return p?.mode==='legacy'&&day.menu?day.menu:pattern[idx%7]}function selectedWeeks(){const ws=weeksNow(),curDay=new Date().toLocaleDateString('sv-SE');if(!nWeek){const hit=ws.find(w=>w.days.some(d=>d.date===curDay));nWeek=hit?.week||1}return ws.filter(w=>w.week>=nWeek&&w.week<nWeek+nSpan)}function dayCards(ws){return ws.map(w=>`<div class="nutrition-week"><div class="nutrition-week-head"><b>Semaine ${w.week}</b><span>${w.phase||''}</span></div>${w.days.map((d,i)=>{const c=codeFor(d,i),l=N.meal(c,'lunch'),di=N.meal(c,'dinner');return `<div class="menu-day"><div class="menu-day-head"><b>${fmtShort(d.date)}</b><span class="tag">Menu ${c}</span></div><div class="meal-grid"><article class="meal-card"><img src="${l.photo}" alt="Illustration ${l.name}" loading="lazy"><div class="meal-body"><small>Déjeuner</small><b>${l.name}</b></div></article><article class="meal-card"><img src="${di.photo}" alt="Illustration ${di.name}" loading="lazy"><div class="meal-body"><small>Dîner</small><b>${di.name}</b></div></article></div></div>`}).join('')}</div>`).join('')}
function shopping(ws){const days=[];for(const w of ws)w.days.forEach((d,i)=>days.push({...d,menu:codeFor(d,i)}));const rows=N.aggregate(days,nPeople),cats={};for(const r of rows)(cats[r.cat]??=[]).push(r);return Object.entries(cats).map(([cat,items])=>`<div class="shop-cat"><h4>${cat}</h4>${items.map(x=>`<label class="checkline"><input type="checkbox"> <span>${x.name}</span><b>${N.fmtQty(x)}</b></label>`).join('')}</div>`).join('')}
function render(){const box=document.querySelector('#nutrition');if(!box)return;const ws=selectedWeeks(),all=weeksNow(),maxW=all.length;const p=profile(),personal=p?.mode==='legacy';box.innerHTML=`<div class="card"><span class="tag">PLAN REPAS</span><h2>Menus sur plusieurs jours</h2><p class="muted">${personal?'Les quantités reprennent ton plan actuel.':'Pour les profils personnalisés, ces menus sont des idées de repas et non une prescription calorique individualisée.'}</p><div class="grid"><div><label>À partir de la semaine</label><select id="nutWeek">${all.map(w=>`<option value="${w.week}" ${w.week===nWeek?'selected':''}>Semaine ${w.week}</option>`).join('')}</select></div><div><label>Afficher</label><select id="nutSpan"><option value="1" ${nSpan===1?'selected':''}>1 semaine</option><option value="2" ${nSpan===2?'selected':''}>2 semaines</option><option value="4" ${nSpan===4?'selected':''}>4 semaines</option></select></div></div><p class="muted small">Photos illustratives. Les plats servis peuvent différer selon les ingrédients et la préparation.</p></div>${dayCards(ws)}<div class="card"><span class="tag">FOYER</span><h2>Liste de courses</h2><div class="grid"><div><label>Nombre de personnes</label><select id="nutPeople">${[1,2,3,4,5,6,7,8].map(x=>`<option value="${x}" ${x===nPeople?'selected':''}>${x}</option>`).join('')}</select></div><div><label>Période</label><div class="metric"><b>${ws.length} semaine${ws.length>1?'s':''}</b><br><small>${ws.length*7} jours</small></div></div></div><div id="shoppingList">${shopping(ws)}</div><button class="btn secondary" id="copyShop">Copier la liste de courses</button></div>`;
box.querySelector('#nutWeek').onchange=e=>{nWeek=+e.target.value;render()};box.querySelector('#nutSpan').onchange=e=>{nSpan=+e.target.value;if(nWeek+nSpan-1>maxW)nSpan=Math.max(1,maxW-nWeek+1);render()};box.querySelector('#nutPeople').onchange=e=>{nPeople=+e.target.value;render()};box.querySelector('#copyShop').onclick=async()=>{const lines=[];box.querySelectorAll('.shop-cat').forEach(c=>{lines.push(c.querySelector('h4').textContent);c.querySelectorAll('.checkline').forEach(l=>lines.push('• '+l.querySelector('span').textContent+' — '+l.querySelector('b').textContent));lines.push('')});const txt=`Rome42 — Liste de courses\n${nPeople} personne${nPeople>1?'s':''} · ${ws.length} semaine${ws.length>1?'s':''}\n\n`+lines.join('\n');try{await navigator.clipboard.writeText(txt);alert('Liste copiée.')}catch(e){if(navigator.share)await navigator.share({title:'Liste de courses Rome42',text:txt});}}}
const btn=document.querySelector('nav button[data-v="nutrition"]');if(btn)btn.addEventListener('click',()=>setTimeout(render,0));window.R42_renderNutrition=render;})();